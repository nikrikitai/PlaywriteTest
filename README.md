# Инструкция по запуску Playwright тестов

Данная инструкция предназначена для ознакомления и запуска Playwright-тестов из репозитория: [https://github.com/nikrikitai/PlaywriteTest](https://github.com/nikrikitai/PlaywriteTest)

---

## 📋 Шаг 1: Подготовка к работе

### 1.1 Проверка наличия Node.js

Перед запуском тестов необходимо проверить, установлен ли **Node.js** — это специальная среда, которая нужна, чтобы запускать тесты на вашем компьютере. Если её не будет, тесты не запустятся.

**Для проверки:**

1. Откройте **Терминал** (или командную строку: `Win + R` → введите `cmd` → Enter)
2. Введите команду:
   ```bash
   node -v
   ```

**Результат:**
- ✅ Если Node.js установлен, вы увидите версию (например, `v20.11.1`) — можно двигаться дальше
- ❌ Если Node.js НЕ установлен, появится сообщение: `'node' не является внутренней или внешней командой` (Windows) или `command not found` (macOS/Linux)

**Ссылка для скачивания Node.js:** [https://nodejs.org/en/download](https://nodejs.org/en/download) (выбирайте LTS-версию)

Рисунок 1 – Проверка node.js
<img width="726" height="72" alt="image" src="https://github.com/user-attachments/assets/92db2ba4-a794-49e6-90ff-01ac1ba3617f" />


---

## 📦 Шаг 2: Получение тестов

### 2.1 Скачивание архива с GitHub

1. Перейдите по ссылке: [https://github.com/nikrikitai/PlaywriteTest](https://github.com/nikrikitai/PlaywriteTest)
2. Нажмите зелёную кнопку **`<> Code`**
3. В меню выберите **`Download ZIP`**

Рисунок 2 – Загрузка ZIP архива репозитория
<img width="912" height="558" alt="image" src="https://github.com/user-attachments/assets/58e06622-0ef8-472d-a22c-aa851f6737de" />


### 2.2 Распаковка архива

Распакуйте скачанный ZIP-архив в любое удобное место на компьютере (например, в папку `C:\Projects\` или на Рабочий стол).

---

## 💻 Шаг 3: Переход в директорию с тестами

1. Откройте терминал (сочетание клавиш `Win+R`, введите `cmd` и нажмите Enter)
2. С помощью команды `cd` перейдите в распакованную папку

**Формат команды:**
```bash
cd [путь-к-проекту]/test-project
```

**Пример:**
```bash
cd C:\Users\ИмяПользователя\Downloads\PlaywriteTest-main\test-project
```

Рисунок 3 – Переход в директорию с тестами
<img width="859" height="109" alt="image" src="https://github.com/user-attachments/assets/2d2d2a79-d187-4455-aff8-feee6e416877" />


---

## 🔧 Шаг 4: Настройка переменных окружения

В папке проекта есть файл **`.env.example`**. Необходимо создать на его основе копию и назвать **`.env`**.

**Для этого:**
1. Найдите в папке файл `.env.example`
2. Нажмите `Ctrl+C`, затем `Ctrl+V`
3. Переименуйте копию в `.env` (просто удалите `.example`)
4. Замените данные внутри файла на данные для тестируемого сайта

### 📄 Содержимое `.env.example` файла:

```env
# URL и настройки
BASE_URL=http://domain.zone                  # базовый URL тестируемого ресурса
LOGIN_ENDPOINT=/identity/...                 # конечная точка веб-сервиса для входа
PAGE_ENDPOINT=/page1                         # конечная точка тестируемой страницы
MAX_LOGIN_ATTEMPTS=5                          # максимальное количество попыток для входа
TIMEOUT_ERROR_S=60                            # время в секундах для снятия ошибки 403
RATE_LIMIT_ATTEMPTS=50                         # максимальное количество попыток для вызова ошибки

# Локаторы и тексты
PAGE_TITLE_REGEX=САЙТ - Войти                 # заголовок страницы регистрации
PAGE_TITLE_MAIN=Welcome                       # заголовок главной страницы
PAGE_TEST_TITLE=Какая-то страница              # заголовок тестируемой страницы
EMAIL_LABEL=Электронная почта
PASSWORD_LABEL=Пароль
BUTTON_TEXT=Войти                             # текст кнопки для входа
BUTTON_TO_PAGE_TEST=Нажми меня                 # кнопка выпадающего списка для перехода на тестируемую страницу
BUTTON_TEXT_TO_TEST01=Лучше нажми меня         # текст первой тестируемой кнопки
BUTTON_TEXT_TO_TEST02=Нет нажми меня           # текст второй тестируемой кнопки
ERROR_MESSAGE_TEXT=Учетная запись заблокирована # текст сообщения с ошибкой

# Данные для входа на сайт
USER_LOGIN=user123@mail.com                   # логин для входа
USER_PASSWORD=12345678                         # пароль для входа
```

---

## 🚀 Шаг 5: Запуск тестов

Если используете сразу 2 теста, рекомендуется запускать их в следующем порядке:
1. `test_queue.spec.js`
2. `login-page.spec.js`

### 5.1 Тест `test_queue.spec.js`

Данный тест предназначен для тестирования вывода ошибки 403.

**Для запуска:**
1. Откройте терминал
2. Перейдите в директорию с тестами
3. Введите команду:
   ```bash
   npm run test:queue
   ```

После окончания теста можно сформировать отчет:
```bash
npx playwright show-report
```

Рисунок 4 – Отчет по тесту test_queue.spec.js
<img width="914" height="326" alt="image" src="https://github.com/user-attachments/assets/6d3f2a2e-3616-4231-9700-7d2ef89f369c" />


### 5.2 Тест `login-page.spec.js`

Данный тест предназначен для тестирования:
- Входа в систему по валидным данным
- Блокировки при входе по неверным данным

**Для запуска:**
```bash
npm run test:login
```

После окончания теста сформируйте отчет:
```bash
npx playwright show-report
```

Рисунок 5 – Отчет по тесту login-page.spec.js
<img width="991" height="369" alt="image" src="https://github.com/user-attachments/assets/1769c889-f5d3-4864-bb88-c37ee1de41d6" />


---

## 📊 Шаг 6: Формирование Allure отчета

Помимо отчетов Playwright, мы можем сформировать более информативный и понятный **Allure отчет**.

**Для его формирования:**

1. Прогоните все тесты
2. В командной строке, находясь в директории с тестами, введите:
   ```bash
   allure generate allure-results -o allure-report --clean
   ```
3. Для вывода отчета введите:
   ```bash
   allure open allure-report
   ```

После выполнения этих команд в браузере, установленном по умолчанию, откроется страница с подробным отчетом по пройденным тестам.

Рисунок 6 – Формирование allure отчета
<img width="932" height="199" alt="image" src="https://github.com/user-attachments/assets/0606edcc-caa2-4892-8776-dd8f4b41cdbe" />

Рисунок 7 – Allure отчет
<img width="969" height="474" alt="image" src="https://github.com/user-attachments/assets/a80ddba6-3e31-4d60-8c1f-1bc7d823f9ca" />

---

## ❓ Возможные проблемы и их решение

| Проблема | Решение |
|----------|---------|
| `node -v` не работает | Установите Node.js с официального сайта |
| Ошибка "Cannot find module" | Выполните `npm install` в директории проекта |
| Тесты падают с ошибкой авторизации | Проверьте логин/пароль в файле `.env` |
| Allure не запускается | Убедитесь, что Java установлена (требуется для Allure) |

---

## 📝 Примечания

- Убедитесь, что все данные в файле `.env` заполнены корректно
- При возникновении проблем проверьте актуальность версий Node.js и npm
